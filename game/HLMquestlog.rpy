label createlog:

    python:

        quests = [ ]

        ####Quest1####
        goals = [ ]
        stages = [ ]
        goals.append(Goal("Station Agent", "Speak with station agent"))

        stages.append(Stage())

        quests.append(Quest())

        del goals
        del quests
        del stages

    return
